Drop the real media assets in this folder using the exact filenames listed
in the project README (asset list). Until real files are added, the site
runs fine — every section shows a clearly labeled placeholder instead of a
broken image/video.
